<div class="row-fluid">
<div class="span12">
<div class="page-header position-relative">
<h1>Kategoriler</h1>
</div>
<%= link_to 'Yeni Kategori', new_admin_category_path, :class=>"btn btn-success", class: "btn btn-success", style: "margin-bottom: 10px" %>
<table class="table table-bordered">
  <thead>
    <tr>
      <th>Başlık</th>
      <th></th>

<th></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>

      <tr>
        <td><%= category.title %></td>
         <td> <%= link_to admin_articles_path(category_id: category.id) do%>
          <i class="icon-list"></i>
          Haberler
        <% end %>
         </td>
        <td><%= link_to admin_category_path(category), class: "btn btn-primary btn-mini", title: "Detay" do %>
           <i class="icon-info-sign"></i>
          
          <% end %>
        <%= link_to edit_admin_category_path(category), class: "btn btn-info btn-mini", title: "Düzenle" do %>
        <i class="icon-edit"></i>
          
        <% end %>
        <%= link_to admin_category_path(category), class: "btn btn-danger btn-mini", method: :delete, data: { confirm: 'Haberi silmek istediginize emin misiniz?' }, title: "Sil" do %>
        <i class="icon-remove"></i>
          
        <% end %>
        </td>
      </tr>

   <%  category.children.each do |category| %>
            <tr>
        <td><%= '- ' *category.level %><%= category.title %></td>
         <td> <%= link_to admin_articles_path(category_id: category.id) do%>
          <i class="icon-list"></i>
          Haberler
        <% end %>
         </td>
        <td><%= link_to admin_category_path(category), class: "btn btn-primary btn-mini", title: "Detay" do %>
           <i class="icon-info-sign"></i>
          
          <% end %>
        <%= link_to edit_admin_category_path(category), class: "btn btn-info btn-mini", title: "Düzenle" do %>
        <i class="icon-edit"></i>
          
        <% end %>
        <%= link_to admin_category_path(category), class: "btn btn-danger btn-mini", method: :delete, data: { confirm: 'Haberi silmek istediginize emin misiniz?' }, title: "Sil" do %>
        <i class="icon-remove"></i>
          
        <% end %>
        </td>
      </tr>
      <%  category.children.each do |category| %>
            <tr>
        <td><%= '- ' *category.level %><%= category.title %></td>
         <td> <%= link_to admin_articles_path(category_id: category.id) do%>
          <i class="icon-list"></i>
          Haberler
        <% end %>
         </td>
        <td><%= link_to admin_category_path(category), class: "btn btn-primary btn-mini", title: "Detay" do %>
           <i class="icon-info-sign"></i>
          
          <% end %>
        <%= link_to edit_admin_category_path(category), class: "btn btn-info btn-mini", title: "Düzenle" do %>
        <i class="icon-edit"></i>
          
        <% end %>
        <%= link_to admin_category_path(category), class: "btn btn-danger btn-mini", method: :delete, data: { confirm: 'Haberi silmek istediginize emin misiniz?' }, title: "Sil" do %>
        <i class="icon-remove"></i>
          
        <% end %>
        </td>
      </tr>
        <%  category.children.each do |category| %>
            <tr>
        <td><%= '- ' *category.level %><%= category.title %></td>
         <td> <%= link_to admin_articles_path(category_id: category.id) do%>
          <i class="icon-list"></i>
          Haberler
        <% end %>
         </td>
        <td><%= link_to admin_category_path(category), class: "btn btn-primary btn-mini", title: "Detay" do %>
           <i class="icon-info-sign"></i>
          
          <% end %>
        <%= link_to edit_admin_category_path(category), class: "btn btn-info btn-mini", title: "Düzenle" do %>
        <i class="icon-edit"></i>
          
        <% end %>
        <%= link_to admin_category_path(category), class: "btn btn-danger btn-mini", method: :delete, data: { confirm: 'Haberi silmek istediginize emin misiniz?' }, title: "Sil" do %>
        <i class="icon-remove"></i>
          
        <% end %>
        </td>
      </tr>
        <%  category.children.each do |category| %>
            <tr>
        <td><%= '- ' *category.level %><%= category.title %></td>
         <td> <%= link_to admin_articles_path(category_id: category.id) do%>
          <i class="icon-list"></i>
          Haberler
        <% end %>
         </td>
        <td><%= link_to admin_category_path(category), class: "btn btn-primary btn-mini", title: "Detay" do %>
           <i class="icon-info-sign"></i>
          
          <% end %>
        <%= link_to edit_admin_category_path(category), class: "btn btn-info btn-mini", title: "Düzenle" do %>
        <i class="icon-edit"></i>
          
        <% end %>
        <%= link_to admin_category_path(category), class: "btn btn-danger btn-mini", method: :delete, data: { confirm: 'Haberi silmek istediginize emin misiniz?' }, title: "Sil" do %>
        <i class="icon-remove"></i>
          
        <% end %>
        </td>
      </tr>
    <% end %>
    <% end %>
    <% end %>

    <% end %>

    <% end %>


  </tbody>
</table>
<%= will_paginate @categories, :renderer=>"BootstrapLinkRenderer" %>



</div>
</div>